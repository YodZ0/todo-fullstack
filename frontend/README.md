# ToDo Sample Frontend

## FSD

The main rule of FSD is that a layer can only import from layers strictly below itself. Hierarchy from top to bottom:

```text
app → pages → widgets → features → entities → shared
```
